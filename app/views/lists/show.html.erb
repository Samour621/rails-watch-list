<h1><%= @list.name %></h1>


<% @list.bookmarks.each do |bookmark| %>
<li><%= bookmark.movie.title %> - <%= bookmark.movie.overview %> - <%= bookmark.movie.rating  %>/10</li>
<button><%= link_to 'Remove', bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></button>
<% end %>
</ul>

<button><%= link_to 'Home', lists_path %></button>
<button><%= link_to 'Add Movie to List', new_list_bookmark_path(@list) %></button>
